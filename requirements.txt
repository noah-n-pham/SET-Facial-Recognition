# Core Dependencies for Face Recognition System

# Deep Learning & Face Recognition
insightface>=0.7.3          # Pretrained face recognition models (MobileFaceNet)
onnx>=1.14.0                # Model export format
onnxruntime>=1.15.0         # ONNX inference engine

# Computer Vision
opencv-python>=4.8.0        # Face detection (YuNet) and image processing

# Numerical Computing
numpy>=1.24.0               # Array operations and embeddings

# Utilities
pyyaml>=6.0                 # Configuration files
tqdm>=4.65.0                # Progress bars
Pillow>=9.0.0               # Image loading

# Hardware (Optional - only needed for Arduino integration)
pyserial>=3.5               # Arduino serial communication

# Note: PyTorch/TorchVision NOT needed - we use pretrained models directly!

filterpy
